---
title: Install Jitsi Meet on Ubuntu Server 20.04
authors:
  - tech-fiddle
  - rudra-sen
tags: ["Ubuntu", "Jitsi", "Server", "Meeting"]
description: Blog Post for Installing Jitsi Meet on Ubuntu Server 20.04.
image: "/img/blog/8/8.jpg"
hide_table_of_contents: false
slug: posts/install-jitsi-meet-on-ubuntu-server-20-04
comments: true
draft: true
---

# Install Jitsi Meet on Ubuntu Server 20.04

Today we'll guide you on Installing Jitsi Meet on Ubuntu Server 20.04.

{/* truncate */}

## Prerequisites

- Ubuntu Server 20.04+
- Internet Access on Server
- Public IP Address (Optional but Recommended)
- `sudo` / `root` User Access

## Introduction

Today we'll install Jitsi Meet Server on an Ubuntu Server. So let me tell you what is **Jitsi Meet**. Jitsi Meet is a Ubuntu Package that can enable you to Attend and Join Meetings using the server. Sounds Cool, Huh?

So Now Let's Begin the Installation...

sudo apt -y update
sudo apt -y install gnupg2 nginx-full curl wget apt-transport-https
sudo apt-add-repository universe
sudo apt -y update

DNS:

- `A` | `mail.techfiddle.org` | `Public IP Address` | `1800`
